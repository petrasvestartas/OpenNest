# OpenNest C++ example

Self-contained. The first build downloads OpenNest and compiles its two native engines from source
(Windows, macOS, Linux — no prebuilt binaries needed).

- **Windows:** double-click `run.bat` (needs CMake + Visual Studio).
- **macOS / Linux:** `./run.command` (needs CMake + a C++17 compiler).
