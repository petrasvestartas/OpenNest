<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>disable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <AssemblyName>example</AssemblyName>
  </PropertyGroup>
  <!-- The native engines (nfp_nest, nest_physics) are built from source by run.bat / run.command and
       copied next to the executable; they are P/Invoked by bare name at runtime. -->
</Project>
