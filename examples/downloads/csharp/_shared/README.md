# OpenNest C# example

Self-contained. The run script downloads OpenNest, builds its two native engines from source, and runs
the example (Windows, macOS, Linux — needs .NET 8 SDK + CMake + a C++ compiler).

- **Windows:** double-click `run.bat`.
- **macOS / Linux:** `./run.command`.
